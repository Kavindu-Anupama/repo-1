CREATE TABLE IF NOT EXISTS sample_one (
    id INT PRIMARY KEY,
    reference VARCHAR(100) NOT NULL,
    amount INT NOT NULL,
    create_as VARCHAR(100) NOT NULL,
    feedback VARCHAR(100) NOT NULL
);